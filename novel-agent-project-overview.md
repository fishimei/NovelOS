# NovelOS 项目总览

这份文档定义当前项目的后端架构方向、资源边界和核心机制。目标不是先实现全部逻辑，而是先把系统应该怎样工作讲清楚，防止实现阶段又退回到“让用户填很多章节参数”的旧思路。

## 1. 项目是什么

NovelOS 是一个“助手建模 + 主控 AI 推进剧情”的小说创作系统。

它分成两层：

- `设定采集层`
  - 用户先说一句模糊想法
  - 助手把它拆成结构化设定草稿
  - 如果缺关键点，助手只问少量高价值问题
  - 用户审校、纠正、补充后，再写入正式项目状态

- `故事推进层`
  - 在正式设定已经存在的前提下
  - 用户每次只给一句推进语
  - 主控 AI 识别冲突轴，生成“具备选择压力”的剧情变量
  - 驱动角色互动，生成正文草稿，再抽取补丁供作者确认

一句话定义：

`作者负责定调与审校，助手负责拆解建模，主控 AI 负责用选择压力推进故事。`

## 2. 为什么不能让用户一次性输入全部设定

一次性填完整世界、人物、关系、变量，对大多数作者来说负担太重。

真实创作通常不是这样发生的。作者更常见的状态是：

- 先有一个感觉
- 先有一个题材方向
- 先有一个人物原型
- 先有一个冲突雏形

但很多结构化信息，是在对话、纠偏、比较和补充中逐渐长出来的。

所以系统不应该要求作者：

- 第一次就写完整力量体系
- 第一次就定义完所有关系
- 第一次就设定完全部世界变量

更合理的方式是：

`作者先说一句话`
-> `助手拆解`
-> `只问关键缺口`
-> `给出结构化草稿`
-> `作者修正`
-> `再正式入库`

所以这个项目里，作者不是填表员，而是：

`设定审校者 + 方向提供者 + 结果确认者`

## 3. 两阶段最小输入原则

### 3.1 设定采集阶段

设定采集阶段的最小输入应该是：

- 一句种子想法 `seed_idea`
- 后续若干轮补充说明 `user_message`
- 对助手拆解结果的纠正意见

例如：

`我想写一个少年成长型玄幻长篇，学院修炼，小队冒险，升级要爽，但每次变强都要付出代价。`

系统不应该在这一步强迫用户填写一长串结构化表单。

### 3.2 故事推进阶段

正式设定完成后，单次推进时最小输入应该是：

- 一句推进语 `author_message`

例如：

`他们在雨夜车站重逢，先试探，再失控。`

系统再根据已有状态，自主决定：

- 当前最值得推进的冲突轴
- 哪个角色最该被逼入选择
- 哪个选择最具戏剧压力

## 4. 系统核心对象

### 4.1 Author Bible

项目的长期创作规则，包含：

- 主题
- 风格指南
- 世界规则
- 审美原则
- 硬约束
- 软偏好
- 禁止套路
- 初始世界变量

### 4.2 Character

角色的长期信息：

- 身份
- 性格
- 说话风格
- 目标
- 恐惧
- 秘密
- 行为约束

### 4.3 Relationship

角色之间的结构化关系状态：

- 关系摘要
- 关系锚点
- 紧张点
- 波动度
- 最近变化摘要

### 4.4 Memory

角色个人记忆。角色默认只基于自己该知道的信息行动，不默认共享全知视角。

### 4.5 World State

世界内部可持续变化的变量，例如：

- 某个阵眼是否损坏
- 某种秘术是否暴露
- 某个宗门是否公开表态

### 4.6 Setup Session

设定采集会话，是从模糊想法到正式设定之间的工作容器。

它承接：

- 初始种子想法
- 用户补充说明
- 助手追问
- 助手拆解出的结构化设定草稿

### 4.7 Setup Run

一次助手拆解过程的异步运行。运行结果应当包含：

- 候选 Author Bible
- 候选 Characters
- 候选 Relationships
- 候选 World State
- 待追问问题
- 助手总结

### 4.8 Story Session

正式进入叙事推进后的工作容器，记录：

- 开场情境
- 作者长期意图
- 最近一次作者推进语
- 当前剧情变量摘要

### 4.9 Story Run

一次实际的故事推进运行。它会产出：

- 剧情变量
- 正文草稿
- 审查结果
- 记忆补丁

### 4.10 Plot Variable

这是系统的核心戏剧单元，不是普通事件。

它至少包含：

- 压力来源
- 焦点角色
- 核心选择
- 选项 A
- 选项 B
- A 的代价
- B 的代价
- 不可逆后果
- 波及角色
- 受压世界变量

## 5. 什么是高质量剧情变量

系统要推进的，不是“发生了一件事”，而是“逼角色做出有代价的选择”。

下面这些都不够：

- `敌人突然攻城`
- `女主发现秘密`
- `系统发布任务`

它们只是事件、信息或机制。

高质量剧情变量必须满足：

- 有明确压力来源
- 有被迫决策的人
- 不能轻松两全
- 每个选项都有损失
- 会改写关系、记忆或世界状态

例如：

- 敌人攻城时，主角必须在救师父和保住阵眼之间选一个。
- 女主发现秘密后，如果说出来会毁掉男主，不说出来会害死无辜者。
- 奖励能救命，但任务目标违背主角底线。

所以关键不是“变量更多”，而是“变量具备选择压力”。

## 6. 两段主流程

### 6.1 设定采集流程

`作者一句种子想法`
-> `创建 Setup Session`
-> `助手拆解题材、审美、世界方向、人物原型`
-> `生成结构化设定草稿`
-> `识别关键缺口`
-> `只提出少量高价值追问`
-> `作者补充 / 纠正`
-> `再次推进 Setup Session`
-> `设定逐步稳定`
-> `作者确认应用到正式项目状态`

这里最重要的是：

`设定先以草稿形式存在，经过作者审校后才进入正式状态。`

### 6.2 故事推进流程

`作者一句推进语`
-> `读取正式 Author Bible`
-> `读取 Characters`
-> `读取 Relationships`
-> `读取 Memories`
-> `读取 World State`
-> `主控 AI 识别当前冲突轴`
-> `生成候选剧情变量`
-> `挑选最具选择压力的变量`
-> `驱动相关角色互动`
-> `生成正文草稿`
-> `审查一致性`
-> `抽取补丁`
-> `作者确认`
-> `写回正史`

这里最重要的是：

`推进故事的最小输入是一句推进语，复杂编排全部留在系统内部。`

## 7. 为什么必须保留草稿与确认

无论是设定采集层还是故事推进层，都不能让 AI 结果直接进入长期状态。

### 7.1 设定采集层

助手拆出来的设定可能：

- 理解偏了
- 推断过头了
- 风格不符合作者意图

所以必须先让用户审校，再应用。

### 7.2 故事推进层

故事草稿也可能：

- 只是一个探索版本
- 有不想保留的关系变化
- 有不想写入的记忆变化

所以必须保留：

`草稿 -> 审查 -> 补丁 -> 作者确认 -> 提交`

## 8. 当前后端资源边界

当前后端已经按这个方向收束为以下资源：

- `projects`
- `author-bible`
- `characters`
- `relationships`
- `setup-sessions`
- `setup-runs`
- `story-sessions`
- `story-runs`
- `chapters`
- `memories`

其中：

- `setup-session` 是设定采集工作容器
- `setup-run` 是一次助手拆解/追问/生成结构稿的运行
- `story-session` 是叙事工作容器
- `story-run` 是一次剧情推进运行

## 9. 当前仍然是 TODO 骨架

这次改造的目标，是先统一：

- 名词体系
- 资源边界
- 状态对象
- 文档逻辑
- API 语义

业务逻辑现在可以仍然保留 TODO。这样做是为了先把后端方向钉住，不再继续围绕旧“章节参数驱动”接口扩展实现。

## 10. 后续实现优先级

第一阶段优先做：

1. `SetupSession` / `SetupRun` 的持久化骨架
2. `StorySession` / `StoryRun` 的持久化骨架
3. `Relationship` 的存取
4. 设定拆解 prompt 拼接器
5. 主控 AI prompt 拼接器
6. 剧情变量生成器
7. 正文草稿生成
8. 补丁抽取

第二阶段再做：

1. 关系变化评分
2. 世界变量检索
3. 审美变量的更细粒度控制
4. 更复杂的多轮角色互动

## 11. 最后的判断标准

后续所有实现，都应该拿这几个问题做自检：

- 这是不是又把内部编排负担推回给用户了？
- 这是不是只是在制造事件，而不是制造选择压力？
- 这个变化会不会污染长期状态？
- 角色是不是只基于自己该知道的信息行动？
- 这一步是不是仍然服务于“高质量剧情变量”？

如果这些问题答不上来，就说明实现方向又开始偏回旧路了。
